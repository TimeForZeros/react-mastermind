import React from 'react';
//we can just use Parentheses so that it creaets
//an implicit return otherwise we would need
//curly braces and return()
const GameBoard = props => (
    <div>
        GameBoard
    </div>
);


export default GameBoard;

